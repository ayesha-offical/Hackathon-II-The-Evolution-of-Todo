# Task: T004 | Spec: Constitution IV - Stateless Backend Architecture
# Description: Environment variable template for development and production
# Purpose: Configuration placeholders for DATABASE_URL and secrets

# ============================================================================
# BACKEND CONFIGURATION
# ============================================================================

# Database connection string (Neon PostgreSQL serverless)
# Format: postgresql://username:password@host:port/database
# Example: postgresql://user:pass@us-east-1.sql.neon.tech/mydb
DATABASE_URL='postgresql://neondb_owner:npg_at01WdvzQgFc@ep-silent-fog-agyx057r-pooler.c-2.eu-central-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require'

# JWT secret for Better Auth integration (Constitution II - JWT Bridge)
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(32))"
# Minimum: 32 characters, random string
BBETTER_AUTH_SECRET=VI5oxGZKnZ7FlLmaw5fGS7t373QzjP2I

# API Configuration
API_PORT=8000
API_HOST=0.0.0.0

# Environment type: development, staging, production
ENVIRONMENT=development

# Logging level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================================================
# FRONTEND CONFIGURATION
# ============================================================================

# Frontend URL for CORS configuration
# Used by backend to allow frontend origin
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000

# Better Auth service URL
# Frontend uses this to authenticate users
NEXT_PUBLIC_BETTER_AUTH_URL=http://localhost:8000/api/v1/auth

# ============================================================================
# DEVELOPMENT ONLY (Do not use in production)
# ============================================================================

# Email service (for development/testing)
# Stub implementation for password reset emails
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=dev
SMTP_PASSWORD=dev

# ============================================================================
# NOTES
# ============================================================================
# 1. Copy this file to .env.local and fill in actual values
# 2. Never commit .env.local to git (add to .gitignore)
# 3. For production: Use environment variable service (Heroku Config Vars, AWS Secrets Manager, etc.)
# 4. DATABASE_URL: Connect to Neon PostgreSQL database
# 5. BETTER_AUTH_SECRET: Must match the secret used by frontend Better Auth library
# 6. JWT tokens expire in 1 hour; refresh tokens valid for 30 days (hardcoded in constants.py)
